<div>


    {{-- <nav class="nav_contend">
        <ul class="li_contend">
            @foreach ($categories as $category)
            <li class="a_contend">
                <a>{{ $category->name }}</a>
            </li>
            @endforeach
        </ul>
    </nav> --}}
    {{-- <nav class="nav nav-pills nav-justified">
        @foreach ($categories as $category)
            <a class="nav-link active">{{ $category->name }}</a>
        @endforeach
    </nav> --}}

